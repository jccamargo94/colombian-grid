{"config":{"lang":["es","en"],"separator":"[\\s\\-\\.]+","pipeline":["stopWordFilter"]},"docs":[{"location":"CONTRIBUTING_DOCS/","title":"Contributing to Documentation","text":"<p>This guide explains how to work with the documentation for <code>colombian-grid</code>.</p>"},{"location":"CONTRIBUTING_DOCS/#documentation-structure","title":"Documentation Structure","text":"<p>The documentation is built using MkDocs with the Material theme.</p> <pre><code>colombian-grid/\n\u251c\u2500\u2500 mkdocs.yml          # MkDocs configuration\n\u251c\u2500\u2500 docs/               # Documentation source files\n\u2502   \u251c\u2500\u2500 index.md        # Home page\n\u2502   \u251c\u2500\u2500 guia.md         # Quick Start Guide\n\u2502   \u251c\u2500\u2500 api-reference.md # API Reference (auto-generated)\n\u2502   \u251c\u2500\u2500 ejemplos.md     # Usage Examples\n\u2502   \u2514\u2500\u2500 assets/         # Images and other assets\n\u2514\u2500\u2500 site/               # Generated documentation (not committed)\n</code></pre>"},{"location":"CONTRIBUTING_DOCS/#building-documentation-locally","title":"Building Documentation Locally","text":""},{"location":"CONTRIBUTING_DOCS/#prerequisites","title":"Prerequisites","text":"<p>Install documentation dependencies using <code>uv</code>:</p> <pre><code>uv sync --group docs\n</code></pre>"},{"location":"CONTRIBUTING_DOCS/#build-the-documentation","title":"Build the Documentation","text":"<p>To build the documentation once:</p> <pre><code>uv run mkdocs build\n</code></pre> <p>This generates the static site in the <code>site/</code> directory.</p>"},{"location":"CONTRIBUTING_DOCS/#serve-the-documentation-locally","title":"Serve the Documentation Locally","text":"<p>To serve the documentation with live reloading:</p> <pre><code>uv run mkdocs serve\n</code></pre> <p>Then open your browser to http://127.0.0.1:8000.</p>"},{"location":"CONTRIBUTING_DOCS/#strict-mode","title":"Strict Mode","text":"<p>To build with warnings treated as errors (recommended before committing):</p> <pre><code>uv run mkdocs build --strict\n</code></pre>"},{"location":"CONTRIBUTING_DOCS/#documentation-guidelines","title":"Documentation Guidelines","text":""},{"location":"CONTRIBUTING_DOCS/#writing-guidelines","title":"Writing Guidelines","text":"<ol> <li>Language: Write documentation in Spanish as the primary audience is Colombian energy market participants.</li> <li>Code Examples: Always include working code examples that can be copy-pasted.</li> <li>Type Hints: Ensure all public API methods have proper return type annotations for auto-generated documentation.</li> <li>Docstrings: Use Google-style docstrings for all public classes and methods.</li> </ol>"},{"location":"CONTRIBUTING_DOCS/#api-reference","title":"API Reference","text":"<p>The API reference is automatically generated from docstrings using <code>mkdocstrings</code>. To add a new class to the API reference:</p> <ol> <li>Add proper docstrings to your class/methods</li> <li>Update <code>docs/api-reference.md</code> with the mkdocstrings directive:</li> </ol> <pre><code>::: colombian_grid.module.ClassName\n    handler: python\n    options:\n      members:\n        - method1\n        - method2\n</code></pre>"},{"location":"CONTRIBUTING_DOCS/#adding-new-pages","title":"Adding New Pages","text":"<ol> <li>Create a new <code>.md</code> file in the <code>docs/</code> directory</li> <li>Add it to the navigation in <code>mkdocs.yml</code>:</li> </ol> <pre><code>nav:\n  - Inicio: index.md\n  - Your New Page: your-page.md\n</code></pre>"},{"location":"CONTRIBUTING_DOCS/#continuous-integration","title":"Continuous Integration","text":"<p>Documentation is automatically built and deployed via GitHub Actions:</p>"},{"location":"CONTRIBUTING_DOCS/#on-pull-requests","title":"On Pull Requests","text":"<ul> <li>Documentation is built in strict mode to catch errors</li> <li>Build artifacts are uploaded for review</li> <li>Any warnings will cause the build to fail</li> </ul>"},{"location":"CONTRIBUTING_DOCS/#on-push-to-main","title":"On Push to Main","text":"<ul> <li>Documentation is built and deployed to GitHub Pages</li> <li>Available at: https://jccamargo94.github.io/colombian-grid/</li> </ul>"},{"location":"CONTRIBUTING_DOCS/#troubleshooting","title":"Troubleshooting","text":""},{"location":"CONTRIBUTING_DOCS/#warning-no-type-or-annotation-for-returned-value","title":"Warning: No type or annotation for returned value","text":"<p>This warning occurs when a method doesn't have a return type annotation. Fix by adding type hints:</p> <pre><code># Before\nasync def get_data(self):\n    return await self.fetcher.get()\n\n# After\nasync def get_data(self) -&gt; list:\n    return await self.fetcher.get()\n</code></pre>"},{"location":"CONTRIBUTING_DOCS/#documentation-not-updating","title":"Documentation not updating","text":"<ol> <li>Clear the site directory: <code>rm -rf site/</code></li> <li>Rebuild: <code>uv run mkdocs build</code></li> </ol>"},{"location":"CONTRIBUTING_DOCS/#missing-dependencies","title":"Missing dependencies","text":"<p>Ensure all documentation dependencies are installed:</p> <pre><code>uv sync --group docs\n</code></pre>"},{"location":"CONTRIBUTING_DOCS/#resources","title":"Resources","text":"<ul> <li>MkDocs Documentation</li> <li>Material for MkDocs</li> <li>mkdocstrings</li> </ul>"},{"location":"api-reference/","title":"Referencia del API","text":"<p>Esta secci\u00f3n contiene la referencia t\u00e9cnica de las clases y m\u00e9todos principales de <code>colombian-grid</code>, generada autom\u00e1ticamente a partir de los docstrings del c\u00f3digo fuente.</p> <p>Note</p> <p>Los docstrings de origen est\u00e1n escritos en ingl\u00e9s, por lo que el contenido generado abajo (firmas, descripciones y ejemplos de cada m\u00e9todo) se muestra en ingl\u00e9s independientemente del idioma de esta p\u00e1gina.</p>"},{"location":"api-reference/#modulo-colombian_gridcorexm","title":"M\u00f3dulo <code>colombian_grid.core.xm</code>","text":"<p>               Bases: <code>AsyncSourceClient</code></p> <p>Asynchronous client for XM API data access.</p> <p>This client provides a high-level interface to fetch electricity market data from the Colombian XM API with automatic data chunking and retry logic.</p> Example <p>async with AsyncXMClient() as client: ...     # Get available metrics ...     metrics = await client.get_available_metrics() ... ...     # Fetch generation data ...     data = await client.get_data( ...         metric=\"Gene\", ...         entity=\"Sistema\", ...         start_date=date(2024, 1, 1), ...         end_date=date(2024, 12, 31) ...     )</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>class AsyncXMClient(AsyncSourceClient):\n    \"\"\"\n    Asynchronous client for XM API data access.\n\n    This client provides a high-level interface to fetch electricity market data\n    from the Colombian XM API with automatic data chunking and retry logic.\n\n    Example:\n        &gt;&gt;&gt; async with AsyncXMClient() as client:\n        ...     # Get available metrics\n        ...     metrics = await client.get_available_metrics()\n        ...\n        ...     # Fetch generation data\n        ...     data = await client.get_data(\n        ...         metric=\"Gene\",\n        ...         entity=\"Sistema\",\n        ...         start_date=date(2024, 1, 1),\n        ...         end_date=date(2024, 12, 31)\n        ...     )\n    \"\"\"\n\n    def __init__(self, timeout: float = 30.0, max_retries: int = 3):\n        \"\"\"\n        Initialize the async XM client.\n\n        Args:\n            timeout: Request timeout in seconds (default: 30.0)\n            max_retries: Maximum number of retries for failed requests (default: 3)\n        \"\"\"\n        super().__init__(timeout=timeout, max_retries=max_retries)\n        self._fetcher = AsyncXMFetcher(self._http_client)\n\n    async def get_available_metrics(self) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch all available metrics from XM API.\n\n        This method retrieves a complete list of available metrics including:\n        - Metric IDs and names\n        - Entity types (Sistema, Recurso, Agente, etc.)\n        - Data granularity (hourly, daily, monthly)\n        - Filter options\n        - Maximum days allowed per request\n\n        Returns:\n            DataFrame with columns: MetricId, MetricName, Entity, MaxDays,\n            Type, Url, Filter, MetricUnits, MetricDescription\n\n        Example:\n            &gt;&gt;&gt; async with AsyncXMClient() as client:\n            ...     metrics = await client.get_available_metrics()\n            ...     # Find generation metrics\n            ...     gen_metrics = metrics[metrics['MetricName'].str.contains('Generaci\u00f3n')]\n        \"\"\"\n        return await self._fetcher.get_available_metrics()\n\n    async def get_data(\n        self,\n        *,\n        metric: str,\n        entity: str,\n        start_date: date,\n        end_date: date,\n        filter_by: Optional[List[str]] = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch data for a specific metric.\n\n        This method automatically handles:\n        - Date range chunking based on API restrictions\n        - Parallel async requests for better performance\n        - Retry logic for failed requests\n        - Data concatenation and cleanup\n\n        Args:\n            metric: Metric ID (e.g., \"Gene\", \"DemaReal\", \"PrecBolsNaci\")\n            entity: Entity type (e.g., \"Sistema\", \"Recurso\", \"Agente\")\n            start_date: Start date for data retrieval\n            end_date: End date for data retrieval\n            filter_by: Optional list of filter values (e.g., resource codes like [\"TBST\", \"GVIO\"])\n\n        Returns:\n            DataFrame containing the requested data\n\n        Raises:\n            ValueError: If metric/entity combination is invalid\n            httpx.HTTPStatusError: If API request fails after retries\n\n        Example:\n            &gt;&gt;&gt; async with AsyncXMClient() as client:\n            ...     # Fetch system-wide generation\n            ...     data = await client.get_data(\n            ...         metric=\"Gene\",\n            ...         entity=\"Sistema\",\n            ...         start_date=date(2024, 1, 1),\n            ...         end_date=date(2024, 1, 31)\n            ...     )\n            ...\n            ...     # Fetch generation by resource with filtering\n            ...     data = await client.get_data(\n            ...         metric=\"Gene\",\n            ...         entity=\"Recurso\",\n            ...         start_date=date(2024, 1, 1),\n            ...         end_date=date(2024, 1, 31),\n            ...         filter_by=[\"TBST\", \"GVIO\"]  # Specific power plants\n            ...     )\n        \"\"\"\n        return await self._fetcher.get_data(\n            metric=metric,\n            entity=entity,\n            start_date=start_date,\n            end_date=end_date,\n            filter_by=filter_by,\n        )\n</code></pre> <p>Synchronous client for XM API data access.</p> <p>This client provides the same interface as AsyncXMClient but with synchronous method calls, making it easier to use in non-async contexts.</p> <p>Note: Uses sequential requests internally, which may be slower than the async client for large date ranges.</p> Example <p>with SyncXMClient() as client: ...     # Get available metrics ...     metrics = client.get_available_metrics() ... ...     # Fetch generation data ...     data = client.get_data( ...         metric=\"Gene\", ...         entity=\"Sistema\", ...         start_date=date(2024, 1, 1), ...         end_date=date(2024, 12, 31) ...     )</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>class SyncXMClient:\n    \"\"\"\n    Synchronous client for XM API data access.\n\n    This client provides the same interface as AsyncXMClient but with\n    synchronous method calls, making it easier to use in non-async contexts.\n\n    Note: Uses sequential requests internally, which may be slower than\n    the async client for large date ranges.\n\n    Example:\n        &gt;&gt;&gt; with SyncXMClient() as client:\n        ...     # Get available metrics\n        ...     metrics = client.get_available_metrics()\n        ...\n        ...     # Fetch generation data\n        ...     data = client.get_data(\n        ...         metric=\"Gene\",\n        ...         entity=\"Sistema\",\n        ...         start_date=date(2024, 1, 1),\n        ...         end_date=date(2024, 12, 31)\n        ...     )\n    \"\"\"\n\n    def __init__(self, timeout: float = 30.0, max_retries: int = 3):\n        \"\"\"\n        Initialize the sync XM client.\n\n        Args:\n            timeout: Request timeout in seconds (default: 30.0)\n            max_retries: Maximum number of retries for failed requests (default: 3)\n        \"\"\"\n        self._http_client = AsyncHttpClient(timeout=timeout, max_retries=max_retries)\n        self._fetcher = SyncXMFetcher(self._http_client)\n\n    def __enter__(self):\n        \"\"\"Context manager entry.\"\"\"\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        \"\"\"Context manager exit.\"\"\"\n        # For sync context, we don't need explicit cleanup\n        pass\n\n    def get_available_metrics(self) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch all available metrics from XM API (synchronous).\n\n        Returns:\n            DataFrame with all available metrics and their metadata\n\n        Example:\n            &gt;&gt;&gt; with SyncXMClient() as client:\n            ...     metrics = client.get_available_metrics()\n            ...     print(metrics[['MetricId', 'MetricName', 'Entity']].head())\n        \"\"\"\n        return self._fetcher.get_available_metrics()\n\n    def get_data(\n        self,\n        *,\n        metric: str,\n        entity: str,\n        start_date: date,\n        end_date: date,\n        filter_by: Optional[List[str]] = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch data for a specific metric (synchronous).\n\n        Args:\n            metric: Metric ID\n            entity: Entity type\n            start_date: Start date for data retrieval\n            end_date: End date for data retrieval\n            filter_by: Optional list of filter values\n\n        Returns:\n            DataFrame containing the requested data\n\n        Example:\n            &gt;&gt;&gt; with SyncXMClient() as client:\n            ...     data = client.get_data(\n            ...         metric=\"DemaReal\",\n            ...         entity=\"Sistema\",\n            ...         start_date=date(2024, 1, 1),\n            ...         end_date=date(2024, 1, 31)\n            ...     )\n        \"\"\"\n        return self._fetcher.get_data(\n            metric=metric,\n            entity=entity,\n            start_date=start_date,\n            end_date=end_date,\n            filter_by=filter_by,\n        )\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.xm.AsyncXMClient.get_available_metrics","title":"<code>get_available_metrics()</code>  <code>async</code>","text":"<p>Fetch all available metrics from XM API.</p> <p>This method retrieves a complete list of available metrics including: - Metric IDs and names - Entity types (Sistema, Recurso, Agente, etc.) - Data granularity (hourly, daily, monthly) - Filter options - Maximum days allowed per request</p> <p>Returns:</p> Type Description <code>DataFrame</code> <p>DataFrame with columns: MetricId, MetricName, Entity, MaxDays,</p> <code>DataFrame</code> <p>Type, Url, Filter, MetricUnits, MetricDescription</p> Example <p>async with AsyncXMClient() as client: ...     metrics = await client.get_available_metrics() ...     # Find generation metrics ...     gen_metrics = metrics[metrics['MetricName'].str.contains('Generaci\u00f3n')]</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>async def get_available_metrics(self) -&gt; pd.DataFrame:\n    \"\"\"\n    Fetch all available metrics from XM API.\n\n    This method retrieves a complete list of available metrics including:\n    - Metric IDs and names\n    - Entity types (Sistema, Recurso, Agente, etc.)\n    - Data granularity (hourly, daily, monthly)\n    - Filter options\n    - Maximum days allowed per request\n\n    Returns:\n        DataFrame with columns: MetricId, MetricName, Entity, MaxDays,\n        Type, Url, Filter, MetricUnits, MetricDescription\n\n    Example:\n        &gt;&gt;&gt; async with AsyncXMClient() as client:\n        ...     metrics = await client.get_available_metrics()\n        ...     # Find generation metrics\n        ...     gen_metrics = metrics[metrics['MetricName'].str.contains('Generaci\u00f3n')]\n    \"\"\"\n    return await self._fetcher.get_available_metrics()\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.xm.AsyncXMClient.get_data","title":"<code>get_data(*, metric, entity, start_date, end_date, filter_by=None)</code>  <code>async</code>","text":"<p>Fetch data for a specific metric.</p> <p>This method automatically handles: - Date range chunking based on API restrictions - Parallel async requests for better performance - Retry logic for failed requests - Data concatenation and cleanup</p> <p>Parameters:</p> Name Type Description Default <code>metric</code> <code>str</code> <p>Metric ID (e.g., \"Gene\", \"DemaReal\", \"PrecBolsNaci\")</p> required <code>entity</code> <code>str</code> <p>Entity type (e.g., \"Sistema\", \"Recurso\", \"Agente\")</p> required <code>start_date</code> <code>date</code> <p>Start date for data retrieval</p> required <code>end_date</code> <code>date</code> <p>End date for data retrieval</p> required <code>filter_by</code> <code>Optional[List[str]]</code> <p>Optional list of filter values (e.g., resource codes like [\"TBST\", \"GVIO\"])</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>DataFrame containing the requested data</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If metric/entity combination is invalid</p> <code>HTTPStatusError</code> <p>If API request fails after retries</p> Example <p>async with AsyncXMClient() as client: ...     # Fetch system-wide generation ...     data = await client.get_data( ...         metric=\"Gene\", ...         entity=\"Sistema\", ...         start_date=date(2024, 1, 1), ...         end_date=date(2024, 1, 31) ...     ) ... ...     # Fetch generation by resource with filtering ...     data = await client.get_data( ...         metric=\"Gene\", ...         entity=\"Recurso\", ...         start_date=date(2024, 1, 1), ...         end_date=date(2024, 1, 31), ...         filter_by=[\"TBST\", \"GVIO\"]  # Specific power plants ...     )</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>async def get_data(\n    self,\n    *,\n    metric: str,\n    entity: str,\n    start_date: date,\n    end_date: date,\n    filter_by: Optional[List[str]] = None,\n) -&gt; pd.DataFrame:\n    \"\"\"\n    Fetch data for a specific metric.\n\n    This method automatically handles:\n    - Date range chunking based on API restrictions\n    - Parallel async requests for better performance\n    - Retry logic for failed requests\n    - Data concatenation and cleanup\n\n    Args:\n        metric: Metric ID (e.g., \"Gene\", \"DemaReal\", \"PrecBolsNaci\")\n        entity: Entity type (e.g., \"Sistema\", \"Recurso\", \"Agente\")\n        start_date: Start date for data retrieval\n        end_date: End date for data retrieval\n        filter_by: Optional list of filter values (e.g., resource codes like [\"TBST\", \"GVIO\"])\n\n    Returns:\n        DataFrame containing the requested data\n\n    Raises:\n        ValueError: If metric/entity combination is invalid\n        httpx.HTTPStatusError: If API request fails after retries\n\n    Example:\n        &gt;&gt;&gt; async with AsyncXMClient() as client:\n        ...     # Fetch system-wide generation\n        ...     data = await client.get_data(\n        ...         metric=\"Gene\",\n        ...         entity=\"Sistema\",\n        ...         start_date=date(2024, 1, 1),\n        ...         end_date=date(2024, 1, 31)\n        ...     )\n        ...\n        ...     # Fetch generation by resource with filtering\n        ...     data = await client.get_data(\n        ...         metric=\"Gene\",\n        ...         entity=\"Recurso\",\n        ...         start_date=date(2024, 1, 1),\n        ...         end_date=date(2024, 1, 31),\n        ...         filter_by=[\"TBST\", \"GVIO\"]  # Specific power plants\n        ...     )\n    \"\"\"\n    return await self._fetcher.get_data(\n        metric=metric,\n        entity=entity,\n        start_date=start_date,\n        end_date=end_date,\n        filter_by=filter_by,\n    )\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.xm.SyncXMClient.get_available_metrics","title":"<code>get_available_metrics()</code>","text":"<p>Fetch all available metrics from XM API (synchronous).</p> <p>Returns:</p> Type Description <code>DataFrame</code> <p>DataFrame with all available metrics and their metadata</p> Example <p>with SyncXMClient() as client: ...     metrics = client.get_available_metrics() ...     print(metrics[['MetricId', 'MetricName', 'Entity']].head())</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>def get_available_metrics(self) -&gt; pd.DataFrame:\n    \"\"\"\n    Fetch all available metrics from XM API (synchronous).\n\n    Returns:\n        DataFrame with all available metrics and their metadata\n\n    Example:\n        &gt;&gt;&gt; with SyncXMClient() as client:\n        ...     metrics = client.get_available_metrics()\n        ...     print(metrics[['MetricId', 'MetricName', 'Entity']].head())\n    \"\"\"\n    return self._fetcher.get_available_metrics()\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.xm.SyncXMClient.get_data","title":"<code>get_data(*, metric, entity, start_date, end_date, filter_by=None)</code>","text":"<p>Fetch data for a specific metric (synchronous).</p> <p>Parameters:</p> Name Type Description Default <code>metric</code> <code>str</code> <p>Metric ID</p> required <code>entity</code> <code>str</code> <p>Entity type</p> required <code>start_date</code> <code>date</code> <p>Start date for data retrieval</p> required <code>end_date</code> <code>date</code> <p>End date for data retrieval</p> required <code>filter_by</code> <code>Optional[List[str]]</code> <p>Optional list of filter values</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>DataFrame containing the requested data</p> Example <p>with SyncXMClient() as client: ...     data = client.get_data( ...         metric=\"DemaReal\", ...         entity=\"Sistema\", ...         start_date=date(2024, 1, 1), ...         end_date=date(2024, 1, 31) ...     )</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>def get_data(\n    self,\n    *,\n    metric: str,\n    entity: str,\n    start_date: date,\n    end_date: date,\n    filter_by: Optional[List[str]] = None,\n) -&gt; pd.DataFrame:\n    \"\"\"\n    Fetch data for a specific metric (synchronous).\n\n    Args:\n        metric: Metric ID\n        entity: Entity type\n        start_date: Start date for data retrieval\n        end_date: End date for data retrieval\n        filter_by: Optional list of filter values\n\n    Returns:\n        DataFrame containing the requested data\n\n    Example:\n        &gt;&gt;&gt; with SyncXMClient() as client:\n        ...     data = client.get_data(\n        ...         metric=\"DemaReal\",\n        ...         entity=\"Sistema\",\n        ...         start_date=date(2024, 1, 1),\n        ...         end_date=date(2024, 1, 31)\n        ...     )\n    \"\"\"\n    return self._fetcher.get_data(\n        metric=metric,\n        entity=entity,\n        start_date=start_date,\n        end_date=end_date,\n        filter_by=filter_by,\n    )\n</code></pre>"},{"location":"api-reference/#modulo-colombian_gridcoreparatec","title":"M\u00f3dulo <code>colombian_grid.core.paratec</code>","text":"<p>               Bases: <code>AsyncSourceClient</code></p> <p>AsyncParatecClient is an asynchronous client for fetching data from the Paratec API. It provides methods to retrieve generation, substation, and transmission line data.</p> <p>Attributes:</p> Name Type Description <code>_http_client</code> <code>AsyncHttpClient</code> <p>An asynchronous HTTP client for making requests.</p> <code>_generator_fetcher</code> <code>GeneratorFetcher</code> <p>A fetcher for retrieving generation data.</p> <code>_transmission_fetcher</code> <code>TransmissionFetcher</code> <p>A fetcher for retrieving transmission data.</p> <code>_hydro_fetcher</code> <code>HydroFetcher</code> <p>A fetcher for retrieving hydrology data.</p> <p>Methods:</p> Name Description <code>get_generation_data</code> <p>Asynchronously retrieves generation data.</p> <code>get_substation_data</code> <p>Asynchronously retrieves substation data.</p> <code>get_transmission_line_data</code> <p>Asynchronously retrieves transmission line data.</p> <code>get_hydro_data</code> <p>Asynchronously retrieves hydrology data.</p> Source code in <code>src/colombian_grid/core/paratec/paratec.py</code> <pre><code>class AsyncParatecClient(AsyncSourceClient):\n    \"\"\"\n    AsyncParatecClient is an asynchronous client for fetching data from the Paratec API.\n    It provides methods to retrieve generation, substation, and transmission line data.\n\n    Attributes:\n        _http_client (AsyncHttpClient): An asynchronous HTTP client for making requests.\n        _generator_fetcher (GeneratorFetcher): A fetcher for retrieving generation data.\n        _transmission_fetcher (TransmissionFetcher): A fetcher for retrieving transmission data.\n        _hydro_fetcher (HydroFetcher): A fetcher for retrieving hydrology data.\n\n    Methods:\n        get_generation_data(): Asynchronously retrieves generation data.\n        get_substation_data(): Asynchronously retrieves substation data.\n        get_transmission_line_data(): Asynchronously retrieves transmission line data.\n        get_hydro_data(): Asynchronously retrieves hydrology data.\n    \"\"\"\n\n    def __init__(self, timeout: float = 10.0, max_retries: int = 3):\n        super().__init__(timeout=timeout, max_retries=max_retries)\n        self._generator_fetcher = GeneratorFetcher(self._http_client)\n        self._transmission_fetcher = TransmissionFetcher(self._http_client)\n        self._hydro_fetcher = HydroFetcher(self._http_client)\n\n    async def get_generation_data(self) -&gt; list:\n        \"\"\"\n        Asynchronously retrieves generation data using the injected generator fetcher.\n\n        Returns:\n            list: A list of generator data dictionaries.\n        \"\"\"\n        return await self._generator_fetcher.get_data()\n\n    async def get_substation_data(self) -&gt; list:\n        \"\"\"\n        Asynchronously retrieves substation data using the transmission fetcher.\n\n        Returns:\n            list: A list of substation data dictionaries.\n        \"\"\"\n        return await self._transmission_fetcher.get_substation_data()\n\n    async def get_transmission_line_data(self) -&gt; list:\n        \"\"\"\n        Retrieves transmission line data.\n\n        Returns:\n            list: A list of transmission line data dictionaries.\n        \"\"\"\n        return await self._transmission_fetcher.get_transmission_line_data()\n\n    async def get_hydro_data(self) -&gt; list:\n        \"\"\"\n        Asynchronously retrieves hydro data using the injected hydro fetcher.\n\n        Returns:\n            list: A list of hydrology data dictionaries.\n        \"\"\"\n        return await self._hydro_fetcher.get_hydro_data()\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.paratec.AsyncParatecClient.get_generation_data","title":"<code>get_generation_data()</code>  <code>async</code>","text":"<p>Asynchronously retrieves generation data using the injected generator fetcher.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of generator data dictionaries.</p> Source code in <code>src/colombian_grid/core/paratec/paratec.py</code> <pre><code>async def get_generation_data(self) -&gt; list:\n    \"\"\"\n    Asynchronously retrieves generation data using the injected generator fetcher.\n\n    Returns:\n        list: A list of generator data dictionaries.\n    \"\"\"\n    return await self._generator_fetcher.get_data()\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.paratec.AsyncParatecClient.get_substation_data","title":"<code>get_substation_data()</code>  <code>async</code>","text":"<p>Asynchronously retrieves substation data using the transmission fetcher.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of substation data dictionaries.</p> Source code in <code>src/colombian_grid/core/paratec/paratec.py</code> <pre><code>async def get_substation_data(self) -&gt; list:\n    \"\"\"\n    Asynchronously retrieves substation data using the transmission fetcher.\n\n    Returns:\n        list: A list of substation data dictionaries.\n    \"\"\"\n    return await self._transmission_fetcher.get_substation_data()\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.paratec.AsyncParatecClient.get_transmission_line_data","title":"<code>get_transmission_line_data()</code>  <code>async</code>","text":"<p>Retrieves transmission line data.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of transmission line data dictionaries.</p> Source code in <code>src/colombian_grid/core/paratec/paratec.py</code> <pre><code>async def get_transmission_line_data(self) -&gt; list:\n    \"\"\"\n    Retrieves transmission line data.\n\n    Returns:\n        list: A list of transmission line data dictionaries.\n    \"\"\"\n    return await self._transmission_fetcher.get_transmission_line_data()\n</code></pre>"},{"location":"api-reference/#colombian_grid.core.paratec.AsyncParatecClient.get_hydro_data","title":"<code>get_hydro_data()</code>  <code>async</code>","text":"<p>Asynchronously retrieves hydro data using the injected hydro fetcher.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of hydrology data dictionaries.</p> Source code in <code>src/colombian_grid/core/paratec/paratec.py</code> <pre><code>async def get_hydro_data(self) -&gt; list:\n    \"\"\"\n    Asynchronously retrieves hydro data using the injected hydro fetcher.\n\n    Returns:\n        list: A list of hydrology data dictionaries.\n    \"\"\"\n    return await self._hydro_fetcher.get_hydro_data()\n</code></pre>"},{"location":"examples/","title":"Ejemplos de Uso","text":"<p>Aqu\u00ed encontrar\u00e1s ejemplos pr\u00e1cticos de c\u00f3mo utilizar <code>colombian-grid</code> para diferentes casos de uso.</p>"},{"location":"examples/#api-de-xm","title":"API de XM","text":""},{"location":"examples/#ejemplo-1-obtener-metricas-disponibles","title":"Ejemplo 1: Obtener M\u00e9tricas Disponibles","text":"<p>Antes de solicitar datos, puedes consultar todas las m\u00e9tricas que ofrece el API de XM.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncXMClient\n\nasync def main():\n    async with AsyncXMClient() as client:\n        metrics = await client.get_available_metrics()\n        print(\"M\u00e9tricas disponibles en el API de XM:\")\n        print(metrics[[\"MetricId\", \"MetricName\", \"Entity\", \"MaxDays\"]])\n\nasyncio.run(main())\n</code></pre>"},{"location":"examples/#ejemplo-2-consultar-generacion-por-recurso-con-filtros","title":"Ejemplo 2: Consultar Generaci\u00f3n por Recurso con Filtros","text":"<p>Puedes filtrar los datos por c\u00f3digos de recursos espec\u00edficos (por ejemplo, plantas de generaci\u00f3n).</p> <pre><code>import asyncio\nfrom datetime import date\nfrom colombian_grid import AsyncXMClient\n\nasync def main():\n    async with AsyncXMClient() as client:\n        # Consultar generaci\u00f3n para las plantas GUAJIRA1 y GUAJIRA2\n        data = await client.get_data(\n            metric=\"Gene\",\n            entity=\"Recurso\",\n            start_date=date(2024, 1, 1),\n            end_date=date(2024, 1, 5),\n            filter_by=[\"GUAJIRA1\", \"GUAJIRA2\"],\n        )\n        print(\"Generaci\u00f3n para GUAJIRA1 y GUAJIRA2:\")\n        print(data)\n\nasyncio.run(main())\n</code></pre>"},{"location":"examples/#ejemplo-3-consulta-sincrona-de-precios-de-bolsa","title":"Ejemplo 3: Consulta S\u00edncrona de Precios de Bolsa","text":"<p>Si prefieres un enfoque s\u00edncrono, puedes usar <code>SyncXMClient</code>.</p> <pre><code>from datetime import date\nfrom colombian_grid import SyncXMClient\n\nwith SyncXMClient() as client:\n    precio_bolsa = client.get_data(\n        metric=\"PrecBolsNaci\",\n        entity=\"Sistema\",\n        start_date=date(2024, 1, 1),\n        end_date=date(2024, 1, 10),\n    )\n    print(\"Precio de Bolsa Nacional (primeros 10 d\u00edas de Enero 2024):\")\n    print(precio_bolsa)\n</code></pre>"},{"location":"examples/#api-de-paratec","title":"API de Paratec","text":""},{"location":"examples/#ejemplo-4-datos-de-generadores-e-hidrologia","title":"Ejemplo 4: Datos de Generadores e Hidrolog\u00eda","text":"<p>Consulta datos sobre generadores y registros hidrol\u00f3gicos.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        generadores = await client.get_generation_data()\n        print(f\"Total de generadores: {len(generadores)}\")\n\n        hidrologia = await client.get_hydro_data()\n        print(f\"Registros de hidrolog\u00eda: {len(hidrologia)}\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"examples/#ejemplo-5-datos-de-subestaciones","title":"Ejemplo 5: Datos de Subestaciones","text":"<p>Consulta el listado de subestaciones registradas en Paratec.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        subestaciones = await client.get_substation_data()\n        print(f\"Total de subestaciones: {len(subestaciones)}\")\n        print(subestaciones[0])\n\nasyncio.run(main())\n</code></pre>"},{"location":"examples/#ejemplo-6-datos-de-lineas-de-transmision","title":"Ejemplo 6: Datos de L\u00edneas de Transmisi\u00f3n","text":"<p>Consulta el listado de l\u00edneas de transmisi\u00f3n.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        lineas = await client.get_transmission_line_data()\n        print(f\"Total de l\u00edneas de transmisi\u00f3n: {len(lineas)}\")\n        print(lineas[0])\n\nasyncio.run(main())\n</code></pre> <p>Tip</p> <p>Todos los m\u00e9todos de <code>AsyncParatecClient</code> (<code>get_generation_data</code>, <code>get_substation_data</code>, <code>get_transmission_line_data</code>, <code>get_hydro_data</code>) se pueden llamar dentro del mismo bloque <code>async with</code>, reutilizando la misma conexi\u00f3n HTTP.</p>"},{"location":"getting-started/","title":"Comenzando","text":"<p><code>colombian-grid</code> te permite consultar datos p\u00fablicos del mercado el\u00e9ctrico colombiano (Paratec y XM) desde Python, sin tener que lidiar directamente con las APIs REST subyacentes.</p>"},{"location":"getting-started/#instalacion","title":"Instalaci\u00f3n","text":"<p>Con <code>pip</code>:</p> <pre><code>pip install colombian-grid\n</code></pre> <p>Con <code>uv</code>:</p> <pre><code>uv add colombian-grid\n</code></pre>"},{"location":"getting-started/#importando-los-clientes","title":"Importando los clientes","text":"<p>Todos los clientes se pueden importar directamente desde el paquete ra\u00edz:</p> <pre><code>from colombian_grid import AsyncParatecClient, AsyncXMClient, SyncXMClient\n</code></pre>"},{"location":"getting-started/#api-de-xm-datos-de-mercado","title":"API de XM \u2014 datos de mercado","text":"<p>El API de XM provee datos del mercado el\u00e9ctrico (generaci\u00f3n, demanda, precios, etc.). Hay dos clientes disponibles: uno as\u00edncrono (<code>AsyncXMClient</code>, recomendado) y uno s\u00edncrono (<code>SyncXMClient</code>), ambos con la misma interfaz.</p>"},{"location":"getting-started/#cliente-asincrono","title":"Cliente as\u00edncrono","text":"<pre><code>import asyncio\nfrom datetime import date\nfrom colombian_grid import AsyncXMClient\n\nasync def main():\n    async with AsyncXMClient() as client:\n        data = await client.get_data(\n            metric=\"Gene\",\n            entity=\"Sistema\",\n            start_date=date(2024, 1, 1),\n            end_date=date(2024, 1, 31),\n        )\n        print(\"Generaci\u00f3n del Sistema (Enero 2024):\")\n        print(data.head())\n\nasyncio.run(main())\n</code></pre>"},{"location":"getting-started/#cliente-sincrono","title":"Cliente s\u00edncrono","text":"<p>Ideal para scripts simples que no requieren concurrencia:</p> <pre><code>from datetime import date\nfrom colombian_grid import SyncXMClient\n\nwith SyncXMClient() as client:\n    data = client.get_data(\n        metric=\"DemaReal\",\n        entity=\"Sistema\",\n        start_date=date(2024, 1, 1),\n        end_date=date(2024, 1, 31),\n    )\n    print(\"Demanda Real del Sistema (Enero 2024):\")\n    print(data.head())\n</code></pre>"},{"location":"getting-started/#api-de-paratec-datos-de-infraestructura","title":"API de Paratec \u2014 datos de infraestructura","text":"<p>El API de Paratec provee datos sobre la infraestructura del sistema el\u00e9ctrico: generadores, subestaciones, l\u00edneas de transmisi\u00f3n e hidrolog\u00eda. Solo tiene cliente as\u00edncrono (<code>AsyncParatecClient</code>).</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        generadores = await client.get_generation_data()\n        print(f\"Se encontraron {len(generadores)} generadores.\")\n\n        subestaciones = await client.get_substation_data()\n        print(f\"Se encontraron {len(subestaciones)} subestaciones.\")\n\nasyncio.run(main())\n</code></pre> <p><code>AsyncParatecClient</code> implementa <code>async with</code>, as\u00ed que la conexi\u00f3n HTTP subyacente se cierra autom\u00e1ticamente al salir del bloque \u2014 no necesitas cerrarla manualmente.</p>"},{"location":"getting-started/#proximos-pasos","title":"Pr\u00f3ximos pasos","text":"<ul> <li>Revisa la secci\u00f3n de Ejemplos para ver m\u00e1s casos de uso, incluyendo subestaciones y l\u00edneas de transmisi\u00f3n.</li> <li>Consulta la Referencia API para ver la firma completa de cada m\u00e9todo.</li> </ul>"},{"location":"","title":"Inicio","text":"Colombian Grid <p>Un paquete de Python para acceder a datos p\u00fablicos del mercado el\u00e9ctrico colombiano.</p>   [![PyPI](https://img.shields.io/pypi/v/colombian-grid)](https://pypi.org/project/colombian-grid/) [![Docs](https://img.shields.io/badge/docs-mkdocs--material-070394)](https://jccamargo94.github.io/colombian-grid/)   <p><code>colombian-grid</code> es una librer\u00eda de Python dise\u00f1ada para facilitar el acceso a datos p\u00fablicos del mercado de electricidad de Colombia, consumiendo directamente el API de Paratec y XM. La librer\u00eda ofrece una interfaz as\u00edncrona para consultar datos de generadores, infraestructura de transmisi\u00f3n, hidrolog\u00eda y datos del mercado.</p>"},{"location":"#caracteristicas-principales","title":"Caracter\u00edsticas Principales","text":"<ul> <li>M\u00faltiples Fuentes de Datos: Accede a datos de Paratec y XM, dos de las principales fuentes de informaci\u00f3n del mercado energ\u00e9tico colombiano.</li> <li>Interfaz As\u00edncrona: Construida con <code>asyncio</code> y <code>httpx</code> para operaciones de I/O no bloqueantes y de alto rendimiento.</li> <li>Validaci\u00f3n de Datos: Usa <code>Pydantic</code> para validar la estructura y tipos de datos de las respuestas del API (opcional).</li> <li>Manejo Autom\u00e1tico de Paginaci\u00f3n: Para consultas en rangos de fechas extensos, la librer\u00eda autom\u00e1ticamente segmenta las peticiones para cumplir con los l\u00edmites del API de XM.</li> <li>Clientes Sincr\u00f3nicos y Asincr\u00f3nicos: Ofrece flexibilidad para integrarse tanto en proyectos as\u00edncronos como en scripts s\u00edncronos tradicionales.</li> </ul>"},{"location":"#como-empezar","title":"\u00bfC\u00f3mo empezar?","text":"<p>Instala el paquete con <code>pip install colombian-grid</code> y sigue la gu\u00eda Comenzando para tu primera consulta a XM o Paratec en pocos minutos.</p>"},{"location":"#tienes-dudas-o-sugerencias","title":"\u00bfTienes dudas o sugerencias?","text":"<p>Consulta nuestra documentaci\u00f3n o abre un issue en nuestro repositorio. Estamos aqu\u00ed para ayudarte. \ud83d\ude0a</p>"},{"location":"en/api-reference/","title":"API Reference","text":"<p>This section contains the technical reference for the main classes and methods of <code>colombian-grid</code>, generated automatically from the source code's docstrings.</p>"},{"location":"en/api-reference/#module-colombian_gridcorexm","title":"Module <code>colombian_grid.core.xm</code>","text":"<p>               Bases: <code>AsyncSourceClient</code></p> <p>Asynchronous client for XM API data access.</p> <p>This client provides a high-level interface to fetch electricity market data from the Colombian XM API with automatic data chunking and retry logic.</p> Example <p>async with AsyncXMClient() as client: ...     # Get available metrics ...     metrics = await client.get_available_metrics() ... ...     # Fetch generation data ...     data = await client.get_data( ...         metric=\"Gene\", ...         entity=\"Sistema\", ...         start_date=date(2024, 1, 1), ...         end_date=date(2024, 12, 31) ...     )</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>class AsyncXMClient(AsyncSourceClient):\n    \"\"\"\n    Asynchronous client for XM API data access.\n\n    This client provides a high-level interface to fetch electricity market data\n    from the Colombian XM API with automatic data chunking and retry logic.\n\n    Example:\n        &gt;&gt;&gt; async with AsyncXMClient() as client:\n        ...     # Get available metrics\n        ...     metrics = await client.get_available_metrics()\n        ...\n        ...     # Fetch generation data\n        ...     data = await client.get_data(\n        ...         metric=\"Gene\",\n        ...         entity=\"Sistema\",\n        ...         start_date=date(2024, 1, 1),\n        ...         end_date=date(2024, 12, 31)\n        ...     )\n    \"\"\"\n\n    def __init__(self, timeout: float = 30.0, max_retries: int = 3):\n        \"\"\"\n        Initialize the async XM client.\n\n        Args:\n            timeout: Request timeout in seconds (default: 30.0)\n            max_retries: Maximum number of retries for failed requests (default: 3)\n        \"\"\"\n        super().__init__(timeout=timeout, max_retries=max_retries)\n        self._fetcher = AsyncXMFetcher(self._http_client)\n\n    async def get_available_metrics(self) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch all available metrics from XM API.\n\n        This method retrieves a complete list of available metrics including:\n        - Metric IDs and names\n        - Entity types (Sistema, Recurso, Agente, etc.)\n        - Data granularity (hourly, daily, monthly)\n        - Filter options\n        - Maximum days allowed per request\n\n        Returns:\n            DataFrame with columns: MetricId, MetricName, Entity, MaxDays,\n            Type, Url, Filter, MetricUnits, MetricDescription\n\n        Example:\n            &gt;&gt;&gt; async with AsyncXMClient() as client:\n            ...     metrics = await client.get_available_metrics()\n            ...     # Find generation metrics\n            ...     gen_metrics = metrics[metrics['MetricName'].str.contains('Generaci\u00f3n')]\n        \"\"\"\n        return await self._fetcher.get_available_metrics()\n\n    async def get_data(\n        self,\n        *,\n        metric: str,\n        entity: str,\n        start_date: date,\n        end_date: date,\n        filter_by: Optional[List[str]] = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch data for a specific metric.\n\n        This method automatically handles:\n        - Date range chunking based on API restrictions\n        - Parallel async requests for better performance\n        - Retry logic for failed requests\n        - Data concatenation and cleanup\n\n        Args:\n            metric: Metric ID (e.g., \"Gene\", \"DemaReal\", \"PrecBolsNaci\")\n            entity: Entity type (e.g., \"Sistema\", \"Recurso\", \"Agente\")\n            start_date: Start date for data retrieval\n            end_date: End date for data retrieval\n            filter_by: Optional list of filter values (e.g., resource codes like [\"TBST\", \"GVIO\"])\n\n        Returns:\n            DataFrame containing the requested data\n\n        Raises:\n            ValueError: If metric/entity combination is invalid\n            httpx.HTTPStatusError: If API request fails after retries\n\n        Example:\n            &gt;&gt;&gt; async with AsyncXMClient() as client:\n            ...     # Fetch system-wide generation\n            ...     data = await client.get_data(\n            ...         metric=\"Gene\",\n            ...         entity=\"Sistema\",\n            ...         start_date=date(2024, 1, 1),\n            ...         end_date=date(2024, 1, 31)\n            ...     )\n            ...\n            ...     # Fetch generation by resource with filtering\n            ...     data = await client.get_data(\n            ...         metric=\"Gene\",\n            ...         entity=\"Recurso\",\n            ...         start_date=date(2024, 1, 1),\n            ...         end_date=date(2024, 1, 31),\n            ...         filter_by=[\"TBST\", \"GVIO\"]  # Specific power plants\n            ...     )\n        \"\"\"\n        return await self._fetcher.get_data(\n            metric=metric,\n            entity=entity,\n            start_date=start_date,\n            end_date=end_date,\n            filter_by=filter_by,\n        )\n</code></pre> <p>Synchronous client for XM API data access.</p> <p>This client provides the same interface as AsyncXMClient but with synchronous method calls, making it easier to use in non-async contexts.</p> <p>Note: Uses sequential requests internally, which may be slower than the async client for large date ranges.</p> Example <p>with SyncXMClient() as client: ...     # Get available metrics ...     metrics = client.get_available_metrics() ... ...     # Fetch generation data ...     data = client.get_data( ...         metric=\"Gene\", ...         entity=\"Sistema\", ...         start_date=date(2024, 1, 1), ...         end_date=date(2024, 12, 31) ...     )</p> Source code in <code>src/colombian_grid/core/xm/xm_client.py</code> <pre><code>class SyncXMClient:\n    \"\"\"\n    Synchronous client for XM API data access.\n\n    This client provides the same interface as AsyncXMClient but with\n    synchronous method calls, making it easier to use in non-async contexts.\n\n    Note: Uses sequential requests internally, which may be slower than\n    the async client for large date ranges.\n\n    Example:\n        &gt;&gt;&gt; with SyncXMClient() as client:\n        ...     # Get available metrics\n        ...     metrics = client.get_available_metrics()\n        ...\n        ...     # Fetch generation data\n        ...     data = client.get_data(\n        ...         metric=\"Gene\",\n        ...         entity=\"Sistema\",\n        ...         start_date=date(2024, 1, 1),\n        ...         end_date=date(2024, 12, 31)\n        ...     )\n    \"\"\"\n\n    def __init__(self, timeout: float = 30.0, max_retries: int = 3):\n        \"\"\"\n        Initialize the sync XM client.\n\n        Args:\n            timeout: Request timeout in seconds (default: 30.0)\n            max_retries: Maximum number of retries for failed requests (default: 3)\n        \"\"\"\n        self._http_client = AsyncHttpClient(timeout=timeout, max_retries=max_retries)\n        self._fetcher = SyncXMFetcher(self._http_client)\n\n    def __enter__(self):\n        \"\"\"Context manager entry.\"\"\"\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        \"\"\"Context manager exit.\"\"\"\n        # For sync context, we don't need explicit cleanup\n        pass\n\n    def get_available_metrics(self) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch all available metrics from XM API (synchronous).\n\n        Returns:\n            DataFrame with all available metrics and their metadata\n\n        Example:\n            &gt;&gt;&gt; with SyncXMClient() as client:\n            ...     metrics = client.get_available_metrics()\n            ...     print(metrics[['MetricId', 'MetricName', 'Entity']].head())\n        \"\"\"\n        return self._fetcher.get_available_metrics()\n\n    def get_data(\n        self,\n        *,\n        metric: str,\n        entity: str,\n        start_date: date,\n        end_date: date,\n        filter_by: Optional[List[str]] = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"\n        Fetch data for a specific metric (synchronous).\n\n        Args:\n            metric: Metric ID\n            entity: Entity type\n            start_date: Start date for data retrieval\n            end_date: End date for data retrieval\n            filter_by: Optional list of filter values\n\n        Returns:\n            DataFrame containing the requested data\n\n        Example:\n            &gt;&gt;&gt; with SyncXMClient() as client:\n            ...     data = client.get_data(\n            ...         metric=\"DemaReal\",\n            ...         entity=\"Sistema\",\n            ...         start_date=date(2024, 1, 1),\n            ...         end_date=date(2024, 1, 31)\n            ...     )\n        \"\"\"\n        return self._fetcher.get_data(\n            metric=metric,\n            entity=entity,\n            start_date=start_date,\n            end_date=end_date,\n            filter_by=filter_by,\n        )\n</code></pre>"},{"location":"en/api-reference/#module-colombian_gridcoreparatec","title":"Module <code>colombian_grid.core.paratec</code>","text":"<p>               Bases: <code>AsyncSourceClient</code></p> <p>AsyncParatecClient is an asynchronous client for fetching data from the Paratec API. It provides methods to retrieve generation, substation, and transmission line data.</p> <p>Attributes:</p> Name Type Description <code>_http_client</code> <code>AsyncHttpClient</code> <p>An asynchronous HTTP client for making requests.</p> <code>_generator_fetcher</code> <code>GeneratorFetcher</code> <p>A fetcher for retrieving generation data.</p> <code>_transmission_fetcher</code> <code>TransmissionFetcher</code> <p>A fetcher for retrieving transmission data.</p> <code>_hydro_fetcher</code> <code>HydroFetcher</code> <p>A fetcher for retrieving hydrology data.</p> <p>Methods:</p> Name Description <code>get_generation_data</code> <p>Asynchronously retrieves generation data.</p> <code>get_substation_data</code> <p>Asynchronously retrieves substation data.</p> <code>get_transmission_line_data</code> <p>Asynchronously retrieves transmission line data.</p> <code>get_hydro_data</code> <p>Asynchronously retrieves hydrology data.</p> Source code in <code>src/colombian_grid/core/paratec/paratec.py</code> <pre><code>class AsyncParatecClient(AsyncSourceClient):\n    \"\"\"\n    AsyncParatecClient is an asynchronous client for fetching data from the Paratec API.\n    It provides methods to retrieve generation, substation, and transmission line data.\n\n    Attributes:\n        _http_client (AsyncHttpClient): An asynchronous HTTP client for making requests.\n        _generator_fetcher (GeneratorFetcher): A fetcher for retrieving generation data.\n        _transmission_fetcher (TransmissionFetcher): A fetcher for retrieving transmission data.\n        _hydro_fetcher (HydroFetcher): A fetcher for retrieving hydrology data.\n\n    Methods:\n        get_generation_data(): Asynchronously retrieves generation data.\n        get_substation_data(): Asynchronously retrieves substation data.\n        get_transmission_line_data(): Asynchronously retrieves transmission line data.\n        get_hydro_data(): Asynchronously retrieves hydrology data.\n    \"\"\"\n\n    def __init__(self, timeout: float = 10.0, max_retries: int = 3):\n        super().__init__(timeout=timeout, max_retries=max_retries)\n        self._generator_fetcher = GeneratorFetcher(self._http_client)\n        self._transmission_fetcher = TransmissionFetcher(self._http_client)\n        self._hydro_fetcher = HydroFetcher(self._http_client)\n\n    async def get_generation_data(self) -&gt; list:\n        \"\"\"\n        Asynchronously retrieves generation data using the injected generator fetcher.\n\n        Returns:\n            list: A list of generator data dictionaries.\n        \"\"\"\n        return await self._generator_fetcher.get_data()\n\n    async def get_substation_data(self) -&gt; list:\n        \"\"\"\n        Asynchronously retrieves substation data using the transmission fetcher.\n\n        Returns:\n            list: A list of substation data dictionaries.\n        \"\"\"\n        return await self._transmission_fetcher.get_substation_data()\n\n    async def get_transmission_line_data(self) -&gt; list:\n        \"\"\"\n        Retrieves transmission line data.\n\n        Returns:\n            list: A list of transmission line data dictionaries.\n        \"\"\"\n        return await self._transmission_fetcher.get_transmission_line_data()\n\n    async def get_hydro_data(self) -&gt; list:\n        \"\"\"\n        Asynchronously retrieves hydro data using the injected hydro fetcher.\n\n        Returns:\n            list: A list of hydrology data dictionaries.\n        \"\"\"\n        return await self._hydro_fetcher.get_hydro_data()\n</code></pre>"},{"location":"en/examples/","title":"Usage Examples","text":"<p>Here you'll find practical examples of how to use <code>colombian-grid</code> for different use cases.</p>"},{"location":"en/examples/#xm-api","title":"XM API","text":""},{"location":"en/examples/#example-1-getting-available-metrics","title":"Example 1: Getting Available Metrics","text":"<p>Before requesting data, you can list every metric the XM API offers.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncXMClient\n\nasync def main():\n    async with AsyncXMClient() as client:\n        metrics = await client.get_available_metrics()\n        print(\"Metrics available in the XM API:\")\n        print(metrics[[\"MetricId\", \"MetricName\", \"Entity\", \"MaxDays\"]])\n\nasyncio.run(main())\n</code></pre>"},{"location":"en/examples/#example-2-querying-generation-by-resource-with-filters","title":"Example 2: Querying Generation by Resource with Filters","text":"<p>You can filter data by specific resource codes (e.g., power plants).</p> <pre><code>import asyncio\nfrom datetime import date\nfrom colombian_grid import AsyncXMClient\n\nasync def main():\n    async with AsyncXMClient() as client:\n        # Query generation for the GUAJIRA1 and GUAJIRA2 plants\n        data = await client.get_data(\n            metric=\"Gene\",\n            entity=\"Recurso\",\n            start_date=date(2024, 1, 1),\n            end_date=date(2024, 1, 5),\n            filter_by=[\"GUAJIRA1\", \"GUAJIRA2\"],\n        )\n        print(\"Generation for GUAJIRA1 and GUAJIRA2:\")\n        print(data)\n\nasyncio.run(main())\n</code></pre>"},{"location":"en/examples/#example-3-sync-query-for-spot-prices","title":"Example 3: Sync Query for Spot Prices","text":"<p>If you prefer a synchronous approach, use <code>SyncXMClient</code>.</p> <pre><code>from datetime import date\nfrom colombian_grid import SyncXMClient\n\nwith SyncXMClient() as client:\n    spot_price = client.get_data(\n        metric=\"PrecBolsNaci\",\n        entity=\"Sistema\",\n        start_date=date(2024, 1, 1),\n        end_date=date(2024, 1, 10),\n    )\n    print(\"National spot price (first 10 days of January 2024):\")\n    print(spot_price)\n</code></pre>"},{"location":"en/examples/#paratec-api","title":"Paratec API","text":""},{"location":"en/examples/#example-4-generator-and-hydrology-data","title":"Example 4: Generator and Hydrology Data","text":"<p>Query generator data and hydrology records.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        generators = await client.get_generation_data()\n        print(f\"Total generators: {len(generators)}\")\n\n        hydrology = await client.get_hydro_data()\n        print(f\"Hydrology records: {len(hydrology)}\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"en/examples/#example-5-substation-data","title":"Example 5: Substation Data","text":"<p>Query the list of substations registered in Paratec.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        substations = await client.get_substation_data()\n        print(f\"Total substations: {len(substations)}\")\n        print(substations[0])\n\nasyncio.run(main())\n</code></pre>"},{"location":"en/examples/#example-6-transmission-line-data","title":"Example 6: Transmission Line Data","text":"<p>Query the list of transmission lines.</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        lines = await client.get_transmission_line_data()\n        print(f\"Total transmission lines: {len(lines)}\")\n        print(lines[0])\n\nasyncio.run(main())\n</code></pre> <p>Tip</p> <p>All <code>AsyncParatecClient</code> methods (<code>get_generation_data</code>, <code>get_substation_data</code>, <code>get_transmission_line_data</code>, <code>get_hydro_data</code>) can be called inside the same <code>async with</code> block, reusing the same HTTP connection.</p>"},{"location":"en/getting-started/","title":"Getting Started","text":"<p><code>colombian-grid</code> lets you query public data from the Colombian electricity market (Paratec and XM) from Python, without dealing with the underlying REST APIs directly.</p>"},{"location":"en/getting-started/#installation","title":"Installation","text":"<p>With <code>pip</code>:</p> <pre><code>pip install colombian-grid\n</code></pre> <p>With <code>uv</code>:</p> <pre><code>uv add colombian-grid\n</code></pre>"},{"location":"en/getting-started/#importing-the-clients","title":"Importing the clients","text":"<p>All clients can be imported directly from the top-level package:</p> <pre><code>from colombian_grid import AsyncParatecClient, AsyncXMClient, SyncXMClient\n</code></pre>"},{"location":"en/getting-started/#xm-api-market-data","title":"XM API \u2014 market data","text":"<p>The XM API provides electricity market data (generation, demand, prices, etc.). Two clients are available: an async one (<code>AsyncXMClient</code>, recommended) and a sync one (<code>SyncXMClient</code>), both sharing the same interface.</p>"},{"location":"en/getting-started/#async-client","title":"Async client","text":"<pre><code>import asyncio\nfrom datetime import date\nfrom colombian_grid import AsyncXMClient\n\nasync def main():\n    async with AsyncXMClient() as client:\n        data = await client.get_data(\n            metric=\"Gene\",\n            entity=\"Sistema\",\n            start_date=date(2024, 1, 1),\n            end_date=date(2024, 1, 31),\n        )\n        print(\"System generation (January 2024):\")\n        print(data.head())\n\nasyncio.run(main())\n</code></pre>"},{"location":"en/getting-started/#sync-client","title":"Sync client","text":"<p>Ideal for simple scripts that don't need concurrency:</p> <pre><code>from datetime import date\nfrom colombian_grid import SyncXMClient\n\nwith SyncXMClient() as client:\n    data = client.get_data(\n        metric=\"DemaReal\",\n        entity=\"Sistema\",\n        start_date=date(2024, 1, 1),\n        end_date=date(2024, 1, 31),\n    )\n    print(\"System demand (January 2024):\")\n    print(data.head())\n</code></pre>"},{"location":"en/getting-started/#paratec-api-infrastructure-data","title":"Paratec API \u2014 infrastructure data","text":"<p>The Paratec API provides data about the electrical system's infrastructure: generators, substations, transmission lines, and hydrology. It only has an async client (<code>AsyncParatecClient</code>).</p> <pre><code>import asyncio\nfrom colombian_grid import AsyncParatecClient\n\nasync def main():\n    async with AsyncParatecClient() as client:\n        generators = await client.get_generation_data()\n        print(f\"Found {len(generators)} generators.\")\n\n        substations = await client.get_substation_data()\n        print(f\"Found {len(substations)} substations.\")\n\nasyncio.run(main())\n</code></pre> <p><code>AsyncParatecClient</code> implements <code>async with</code>, so the underlying HTTP connection closes automatically when the block exits \u2014 you don't need to close it manually.</p>"},{"location":"en/getting-started/#next-steps","title":"Next steps","text":"<ul> <li>Check the Examples page for more use cases, including substations and transmission lines.</li> <li>See the API Reference for the full signature of every method.</li> </ul>"},{"location":"en/","title":"Home","text":"Colombian Grid <p>A Python package for accessing public data from the Colombian electricity market.</p>   [![PyPI](https://img.shields.io/pypi/v/colombian-grid)](https://pypi.org/project/colombian-grid/) [![Docs](https://img.shields.io/badge/docs-mkdocs--material-070394)](https://jccamargo94.github.io/colombian-grid/)   <p><code>colombian-grid</code> is a Python library designed to make it easy to access public data from the Colombian electricity market, consuming the Paratec and XM APIs directly. The library offers an async-first interface to query generator, transmission infrastructure, hydrology, and market data.</p>"},{"location":"en/#key-features","title":"Key Features","text":"<ul> <li>Multiple Data Sources: Access data from Paratec and XM, two of the main sources of information in the Colombian energy market.</li> <li>Async Interface: Built with <code>asyncio</code> and <code>httpx</code> for non-blocking, high-performance I/O.</li> <li>Data Validation: Uses <code>Pydantic</code> to validate the structure and types of API responses (optional).</li> <li>Automatic Chunking: For large date ranges, the library automatically splits requests to respect XM's API limits.</li> <li>Sync and Async Clients: Flexible enough to integrate into both async projects and traditional sync scripts.</li> </ul>"},{"location":"en/#getting-started","title":"Getting started","text":"<p>Install the package with <code>pip install colombian-grid</code> and follow the Getting Started guide to run your first XM or Paratec query in a few minutes.</p>"},{"location":"en/#questions-or-suggestions","title":"Questions or suggestions?","text":"<p>Check out our documentation or open an issue in our repository. We're here to help. \ud83d\ude0a</p>"}]}